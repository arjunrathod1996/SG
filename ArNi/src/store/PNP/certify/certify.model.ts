

// Define the interface with properties
export interface CertifyModel {
    selectedDocument: undefined;
};

// Initialize the object based on the interface
export const certifyModel : CertifyModel = {
    selectedDocument: undefined,
}